###############################################################
#                ENVIRONMENT CONFIG TEMPLATE                  #
#        (Do NOT commit real credentials to Git)              #
###############################################################

# ==== MongoDB Connection String ====
# For local Mongo on VPS (if installed locally):
# MONGO_URI=mongodb://localhost:27017/employee_dashboard
# For MongoDB Atlas (recommended for production):
# Format: mongodb+srv://<USER>:<PASSWORD>@<CLUSTER>.mongodb.net/employee_dashboard?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://<USERNAME>:<PASSWORD>@<CLUSTER>.mongodb.net/employee_dashboard?retryWrites=true&w=majority

# ==== Server Configuration ==== 
# Railway/Render/Heroku will inject PORT automatically. On Hostinger VPS you choose (e.g. 5000)
PORT=5000
NODE_ENV=production

# ==== Security / Auth ====
# Generate a long random secret: openssl rand -hex 48
JWT_SECRET=REPLACE_WITH_SECURE_RANDOM_HEX

# ==== CORS (Comma-separated origins) ====
# Example: https://app.example.com,https://admin.example.com
ALLOWED_ORIGINS=

# ==== Rate Limiting ====
# Max requests per 15m window (default 100 in production if unset)
API_RATE_LIMIT=100

# ==== Optional Mongo Timeouts (ms) ====
# Keep low if platform kills slow cold starts. Increase if cluster is far from VPS region.
MONGO_SERVER_SELECTION_TIMEOUT_MS=7000
MONGO_CONNECT_TIMEOUT_MS=7000

# ==== Gemini API (Optional feature) ====
GEMINI_API_KEY=

# ==== File Upload / Paths (adjust only if customizing) ====
#UPLOAD_DIR=uploads

# ================= NOTES FOR HOSTINGER VPS ==================
# 1. Copy this file to .env and fill real values.
# 2. Never expose .env via Nginx (serve only /public or frontend build dir if you create one).
# 3. Restart PM2 after changes: pm2 restart guardian-dashboard
# 4. Keep JWT_SECRET & Mongo credentials private.
# ============================================================