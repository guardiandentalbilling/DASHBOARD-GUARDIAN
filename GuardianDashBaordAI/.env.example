# Guardian Dental Dashboard - Environment Configuration
# Copy this to .env for development or customize for production

# Environment Mode
NODE_ENV=development

# Server Configuration
PORT=5000

# MongoDB Configuration
# Development: local MongoDB instance
MONGO_URI=mongodb://localhost:27017/guardian_dental_db

# Production: MongoDB Atlas (recommended)
# MONGO_URI=mongodb+srv://guardian_admin:SECURE_PASSWORD@cluster0.mongodb.net/guardian_dental_db?retryWrites=true&w=majority

# MongoDB Connection Timeouts
MONGO_SERVER_SELECTION_TIMEOUT_MS=10000
MONGO_CONNECT_TIMEOUT_MS=10000

# JWT Configuration (MUST be changed for production)
JWT_SECRET=development_jwt_secret_change_for_production

# Security Configuration
BCRYPT_ROUNDS=10

# Domain Configuration (Update for your deployment)
FRONTEND_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000,http://127.0.0.1:5000

# File Upload Configuration
MAX_FILE_SIZE=10485760  # 10MB
UPLOAD_PATH=/uploads

# API Rate Limiting (requests per 15 minutes per IP)
API_RATE_LIMIT=1000

# Logging Configuration
LOG_LEVEL=debug

# Gemini AI Configuration (Optional - for AI features)
# Get your key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here